1.实现了A-Z，0-9以及各个标点符号的输入和回显

2.支持大小写，包括shift组合键以及大写锁定，同时支持shift+capslock仍然为小写

3.支持回车换行

4.支持空格键和Tab键，且Tab键实现为制表符（即退格时会退四格）

5.支持删除退格，退格如果退回上一行，将退回改行编辑的最后一个字符（空格和Tab键入也是编辑的字符，不会被删除）

6.每隔大约20秒自动清屏

7.按下F1-F5，切换不同颜色，F1=灰白，F2=蓝色，F3=红色, F4=黄色，F5=青色

8.支持一种特殊的组合键，当shift+tab同时按下时，输入W，S，X，E，D，C键时，显示其右边一个键的内容。
原要求是输入“QAZWSX”，由于在记事本上试验时，键盘上按下左shift+tab键，不能输出QAZ，而按下右shift+tab ,不能输出Z，实在是不知道为什么会这样_(:з」∠)_，所以擅自改成了WSXEDX，只是为了功能看起来显得完整一点，请助教大大手下留情。

9.支持make run指令